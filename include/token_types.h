enum token_type {
    IDENTIFIER,
    KEYWORD,
    SEPARATOR,
    OPERATOR,
    LITERAL,
    COMMENT
};

enum keyword {
    PROGRAM,
    FUNCTION,
    INTEGER,
    LONGINT,
    BEGIN,
    END,
    IF, 
    THEN,
    ELSE,
    VAR,
    FOR,
    TO,
    DO
};